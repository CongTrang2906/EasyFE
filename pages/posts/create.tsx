import { Box } from "@mui/system";
import * as React from "react";

export interface CreatePostPageProps {}

export default function CreatePostPageProps(props: CreatePostPageProps) {
  return <Box>Create Post Page</Box>;
}
